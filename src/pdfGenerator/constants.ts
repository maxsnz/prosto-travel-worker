export const IMAGE_MAX_WIDTH = 100; // Should match the value in generatePdfFromMarkdown
export const PAGE_MARGIN = 40; // Standard page margin in points (for A4, 595pt width)
export const PAGE_WIDTH = 595; // A4 width in points
